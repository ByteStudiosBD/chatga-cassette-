name: Deploy to Byte-Dev
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: [self-hosted, byte-dev, docker]
    steps:
      - name: Deploy chatga-cassette-
        run: /opt/deploy-agent/scripts/redeploy.sh chatga-cassette-
